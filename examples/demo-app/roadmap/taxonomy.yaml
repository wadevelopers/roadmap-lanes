# Taxonomy for DemoApp.
# The time fixture includes real zones to exercise overlap and gate summaries.

areas:
  product:
    zones: [checkout, billing, reporting, search]
  platform:
    zones: [api, auth, worker]
